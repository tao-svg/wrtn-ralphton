{
  "name": "wrtn-ralphton",
  "version": "0.0.0",
  "private": true,
  "description": "Onboarding Agent monorepo (PRD-MVP-SLIM v0.10)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "electron"
    ]
  }
}
